<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:orientation="vertical"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin" tools:context=".MainActivity">

    <Button
        style="@style/MenuButton"
        android:id="@+id/study_pizzas_button"
        android:text="Study Pizzas"
        android:onClick="studyPizzas"
        />
    <Button
        style="@style/MenuButton"
        android:id="@+id/study_salads_button"
        android:text="Study Salads"
        android:onClick="studySalads"
        />
    <Button
        style="@style/MenuButton"
        android:id="@+id/study_pastas_button"
        android:text="Study Pastas"
        android:onClick="studyPastas"
        />

</LinearLayout>
